[build]
  command = "curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y --default-toolchain stable && source $HOME/.cargo/env && cargo install dioxus-cli --locked && dx build --release"
  publish = "target/dx/telcoin-explorer/release/web/public"

[build.environment]
  RUST_LOG = "info"
